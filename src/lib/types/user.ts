// types/user.ts
export interface User {
    id: number;
    username: string;
    email: string;
    uid: string
    cart_id : number
    image_url?: string;
    // Add other user fields as necessary
  }
  